# 更新日志

发布流程（GitHub Actions）会在打 `v*` 标签时自动提取**当前版本对应的小节**作为 Release 说明。新增版本时请按下面的格式在本文件顶部添加小节。

格式：

```markdown
## [版本号] - 日期

### 新增 / 修复 / 其他
- 条目
```

## [0.1.23] - 2026-08-11

### 安全
- 收紧 HLS 本地代理的分片、AES 密钥和初始化段索引路由，只接受 ASCII 非负十进制整数；负数、字母、带符号文本和 Unicode 数字不再进入业务 handler。

### 修复
- 为五类索引 handler 增加统一的范围校验，非法、超长或越界索引现在稳定返回 HTTP 404，不再因整数转换异常返回 500，也不会利用 Python 负索引错误读取最后一个媒体资源。

### 测试
- 新增代理索引输入回归测试，覆盖合法/前导零索引、负数、字母、正号、Unicode 数字、超长整数、越界值，以及路由层在调用 handler 前拒绝非法路径；Windows CI 测试总数增至 108 项。

## [0.1.22] - 2026-08-11

### 修复
- 修复 DLNA 时间解析把 `00:00:00` 当成未实现的问题；现在 seek 到 0 秒可以正常传递。

### 测试
- 新增 DLNA 时间解析回归测试，覆盖零秒、带空白的合法时间和 `NOT_IMPLEMENTED` 哨兵；Windows CI 测试总数增至 98 项。

## [0.1.21] - 2026-08-11

### 修复
- 修复媒体缓存并发 miss 时的重复上游请求：AES-128 密钥、混合 HLS 分片、图片转码和直链整段缓冲现在按资源使用 single-flight，同一资源的并发请求共享一个生产任务及结果。
- 混合 HLS 后台预热与播放器即时分片请求会先合流到同一个下载任务，避免播放器追上尚未完成的预热时并发抓取相同分片；若无重试、无播放器请求头的预热失败，前台请求会再用自己的 headers 和正常重试策略串行接管，避免播放可靠性被后台预热降级。预热时序和首片最长等待边界保持不变。
- 单个播放器请求取消不会连带取消仍被其他请求等待的共享下载；切换媒体、停止代理或退出应用时仍会统一取消并等待所有生产任务。失败结果不会写入缓存或长期占位，后续请求可以正常重试。

### 测试
- 新增缓存 single-flight 回归测试，覆盖密钥请求取消隔离、并发失败共享与后续重试、预热/即时分片合流、低重试预热失败后的前台串行接管、图片转换去重和直链缓冲去重；Windows CI 测试总数增至 91 项。

## [0.1.20] - 2026-08-10

### 修复
- 混合 HLS 初始化不再串行预热最多 5 个分片：启动阶段只等待首片完成，让播放器首次请求直接命中；分片 1–3 会在等待首片之前注册为受管后台任务，利用首片网络等待时间并发预取，避免 SetAVTransportURI / SOAP 响应被多个最长 60 秒的下载连续阻塞，也减少播放器紧接着请求下一片时的冷启动等待。
- 后台预热继续由代理自身的任务注册表持有；切换媒体、停止代理或退出应用时会统一取消并等待，不遗留网络请求或 aiohttp session。

### 测试
- 新增混合 HLS 启动预热回归测试，验证分片 0 尚未完成时后续分片已经并发启动、初始化仍只等待分片 0，并可在代理停止时完整取消剩余任务；Windows CI 测试总数增至 85 项。

## [0.1.19] - 2026-08-10

### 安全
- 为 HLS 分片、初始化段和直链媒体的流式上游请求增加独立超时：播放总时长不受限制，但连接/连接池等待最多 10 秒，连续 30 秒收不到上游数据就终止转发，避免慢速上游长期占用代理连接。
- 固定并测试代理连接池总连接数 64、单一上游主机 16 的上限，防止同一慢速媒体源耗尽全部连接。

### 修复
- 流式转发在本地响应开始前读取超时时返回 HTTP 504，其他上游读取错误返回 502；从首包读取、`stream.prepare()` 到传输完成的整个本地请求生命周期都处于统一的上游 response 清理边界内，请求取消时会立即关闭 response 并继续传播取消，不遗留连接。

### 测试
- 新增流式转发超时参数、首包读取超时、`stream.prepare()` 取消、传输中读超时、取消清理和连接池限制回归测试；Windows CI 测试总数增至 84 项。

## [0.1.18] - 2026-08-10

### 修复
- 修复 DLNA 播放失败时把 `ERROR_OCCURRED` 错写到 `TransportState` 的协议语义错误；错误现在写入正确的 `TransportStatus`，播放状态始终保持为规范允许的值。
- RendererBridge 会在 DLNA 服务停止期间保留当前 `TransportStatus` 并在重启后恢复；新的投屏尝试会先清除旧错误，若 URL 校验、SSRF 防护或代理初始化失败则重新报告错误。
- 连续投屏的 SetURI 媒体切换改为串行事务，避免较早请求晚完成后覆盖较新的播放、代理引用或错误状态；URI/元数据和本地媒体代理都采用候选—提交模型，新代理创建或 Player 接受失败时保留旧代理继续运行，成功后才切换引用并停止旧代理。

### 测试
- 新增并更新 DLNA 错误状态回归测试，覆盖 URL 拒绝、代理返回失败、DNS/SSRF 拒绝、意外异常、成功重试、并发 SetURI 串行化、失败 URI 回滚、候选代理清理、旧代理保留/成功退休及服务重启后的状态恢复；Windows CI 测试总数增至 78 项。

## [0.1.17] - 2026-08-10

### 修复
- 修复 mpv 事件线程与应用退出之间的状态竞态：播放器缓存状态统一加锁，shutdown 会先阻止晚到回调并切断实例可见性，再等待 mpv 事件线程退出；工作线程事件经显式 Qt queued dispatcher 回到主线程后才通知 UI/DLNA，且不再在播放器状态锁内执行外部接收者。
- 修复 DLNA 服务停止后 RendererBridge 仍连接播放器信号并持有旧 service 的问题；服务重启时会立即从持续播放的 Player/Bridge 恢复传输状态、进度、时长、原始投屏地址/元数据、音量和静音，不再用新 service 的默认音量覆盖播放器。
- 主窗口默认尺寸恢复为固定 `1200×800` Qt 窗口像素，不再按屏幕 DPR 二次换算；旧版 v1-v6 窗口基准统一退场，首次升级会重置一次旧几何，之后仍正常记住用户通过拖拽边框调整的尺寸与位置。

### 测试
- 新增 mpv shutdown 回调屏障、真实工作线程到 Qt 主线程的信号投递、RendererBridge 完整状态恢复和窗口几何迁移回归测试；Windows CI 测试总数增至 74 项。

## [0.1.16] - 2026-08-10

### 修复
- 修复 DLNA 服务反复启停时 SSDP daemon 线程可能残留的问题：停止时发送 byebye、唤醒接收并有界等待线程退出，超时会保留 listener 引用供后续重试。
- SSDP 的多播发送与 socket 清理由同一把锁串行化；alive 在取得锁后会再次确认运行状态，避免客户端收到 byebye 后又收到新的 alive。
- DLNA 启动现在会等待 SSDP listener 明确完成初始化；socket 建立、bind 等未被容错处理的启动异常会向上层传播并进入统一失败清理，不再因监听线程启动失败而错误显示服务已运行。
- SSDP 启动等待和停止 join 都移到工作线程执行，异常网络环境下不会阻塞 qasync/Qt 主事件循环或冻结界面。

### 测试
- 新增 SSDP 启停、启动失败、alive/byebye 竞态、事件循环可调度、资源关闭及停止超时回归测试；Windows CI 测试总数增至 69 项。

## [0.1.15] - 2026-08-09

### 修复
- 统一管理后台异步任务生命周期：应用退出时会取消并等待所有受管任务结束，集中记录后台异常，避免任务被提前回收或在退出后继续运行。
- 完整关闭当前 HLS / 直链媒体代理：退出应用时会先取消 HLS 预热任务，再关闭代理会话和监听资源，避免遗留网络连接。
- 修复 DLNA 默认自动启动失败时缺少统一清理的问题；手动启停和自动启动现在共用同一套失败收口与界面状态恢复逻辑。
- 修复底层 HTTP 服务停止失败后错误丢弃资源引用的问题：失败时保留服务对象和运行状态，允许后续重试清理，避免重复启动时撞端口。
- 网络响应读取改为在取消和异常路径中也始终关闭 response，减少连接残留。
- 修复应用退出时 DLNA 状态轮询任务可能只被取消、却无法等待结束的问题：活动指针与生命周期所有权分离，由任务注册表保留引用，并在最终退出阶段完成等待。

### 测试
- 新增异步任务、服务启停、代理退出和轮询清理回归测试；Windows CI 当前共执行 62 项测试。

## [0.1.14] - 2026-08-09

### 重要升级提示
- 如果当前安装的是 v0.1.13 或更早版本，请从本 Release 页面手动下载并安装 v0.1.14。旧版本的自动更新器尚未具备可信校验能力；完成这次手动升级后，后续更新才会强制执行 SHA-256 完整性校验。

### 安全
- 自动更新加入 SHA-256 强制校验：安装包可通过镜像下载，但校验信息只从 GitHub 官方接口获取；校验缺失或不匹配时拒绝运行安装包。
- 加固投屏媒体代理：仅允许 HTTP(S)，逐跳检查重定向，并在实际连接阶段过滤回环、link-local、云元数据、CGNAT、保留地址及 DNS rebinding 等绕过。
- mpv 只访问本机代理地址；代理失败或 URL 被安全策略拒绝时不再回退访问原始上游链接。
- 加固 HLS 与图片处理：严格校验 16 字节 AES-128 密钥，限制播放列表和探测读取大小，阻止 Pillow 解压炸弹，并为缓存和连接池增加上限。
- 关闭 mpv 外部配置和脚本自动加载，减少投屏来源可触发的播放器能力。

### 修复
- 修复 DLNA 服务运行地址刷新逻辑中的无效属性写入。
- 修复代理失败后仍可能显示或播放旧媒体的状态不一致问题。
- 修正被安全策略拦截时的中英文提示，区分始终禁止的高风险地址与可配置的普通私网媒体源。

### 测试与兼容性
- 新增 50 项安全回归测试，并在 Pull Request 与正式发布构建前运行 Windows 测试门禁。
- 「允许内网投屏源」默认开启，以兼容手机和 NAS；这会允许电脑可达的普通 RFC1918 / ULA 私网媒体源。可在设置中关闭并进入严格模式。回环、link-local 和云元数据等高风险地址始终禁止。
- 直播 HLS 当前不再绕过安全代理交给 mpv 直接播放；遇到无 `#EXT-X-ENDLIST` 的直播播放列表时会明确报错。直播投屏仍属于规划中能力。

## [0.1.13] - 2026-08-09

### 修复
- 点控制栏按钮切换全屏时白边框/闪烁（双击画面不触发）：点击按钮会激活悬浮控制栏窗口 / 让按钮获得焦点，窗口激活切换导致 DWM 帧重绘闪白——控制栏所有控件设为 `NoFocus`，悬浮栏窗口加 `WindowDoesNotAcceptFocus`（WS_EX_NOACTIVATE，点击永不夺走主窗口激活）

## [0.1.12] - 2026-08-09

### 修复
- 点击「检查更新」页面跳回顶部（真根因）：真实鼠标点击后按钮获得焦点，检查期间按钮被禁用 → 焦点逃逸到其他控件 → ScrollArea 自动滚动到新焦点控件（实测 558→93）。按钮改为 `NoFocus`，点击不受影响、禁用不触发焦点滚动
- 全屏切换白边框/闪白：qframelesswindow 无边框窗口保留 DWM 绘制的缩放边框，颜色跟随 Windows 主题（系统浅色时闪白）——强制 DWM 沉浸式深色帧（`DWMWA_USE_IMMERSIVE_DARK_MODE`）+ 窗口不透明绘制，边框与背景全程深色

## [0.1.11] - 2026-08-09

### 修复
- 全屏切换/退出瞬间整体闪浅色（真根因）：MSFluentWindow 默认启用 **Mica 背景特效**，全屏/还原时 DWM 重建窗口表面、Mica 失效回退系统浅色背景——已禁用 Mica，纯 QSS 背景两种状态表现一致
- **点击进度条/控制栏后键盘快捷键全部失效**（空格/方向键/Esc）：点击 Slider 或悬浮控制栏后焦点离开播放器页，`keyPressEvent` 收不到按键——改为**应用级按键过滤器**（不依赖焦点）：空格暂停、方向键 seek/音量、Esc/F 全屏；焦点在文本输入框/下拉框时按键原样放行（设置页输入不受影响）；Esc/F 仅在播放器页可见时生效

## [0.1.10] - 2026-08-09

### 修复
- 全屏切换/退出瞬间画面闪浅色：mpv 渲染区（原生窗口）在 resize 时用系统默认背景刷一帧——改为自绘纯黑背景 + 不透明绘制
- 点击「检查更新」后设置页滚动位置跳回顶部：按钮检查中文本变长导致卡片列宽变化、水平滚动条出现/消失，垂直位置被 clamp——按钮固定宽度
- 点击「下载更新」后取消，按钮一直显示「正在检查…」：进度对话框新增**取消按钮**，取消立即终止下载任务并清理半成品，按钮随即恢复

## [0.1.9] - 2026-08-09

### 修复
- 双击全屏仍误触发暂停（真根因）：双击序列的**第二次 release** 会重新启动单击判定定时器，导致双击后 560ms 仍触发一次暂停——现在双击（DblClick 事件）会抑制紧随其后的 release，双击后单击正常
- 嵌入式控制栏样式：QSS 改用类选择器限定（此前 `QWidget` 通配到时间标签/下拉框，与半透明背景二次混合成黑底），时间标签透明、倍速下拉改用 Fluent 组件

### 改进
- 设置页「更新下载加速」从自动更新卡片拆分为独立卡片（直连与镜像并行探测、自动选最快）

## [0.1.8] - 2026-08-08

### 修复
- 双击全屏时第一次单击误触发暂停：单击判定定时器改为跟随系统双击判定窗口（`doubleClickInterval + 60ms`），慢速双击不再误触
- 进度条点击定位修复真正生效：此前修在未使用的旧组件上，实际控制栏仍点不动——现悬浮/嵌入式控制栏都支持点击轨道直接定位

### 改进
- 控制栏双形态：非全屏使用**嵌入式控制栏**（在页面布局内，随窗口自然移动缩放，彻底消除拖窗口边缘时悬浮条抖动）；全屏使用悬浮式控制栏（自动隐藏）
- 全屏时鼠标自动隐藏：3 秒无操作后隐藏鼠标与控制栏，动一下即恢复
- 更新下载智能选源：并行探测 GitHub 直连与加速镜像（gh-proxy.com / ghfast.top / ghproxy.net），自动选最快的源；中途失败自动换源；可在设置中关闭镜像
- 清理无引用的旧组件（media_controls / player_window）

## [0.1.7] - 2026-08-08

### 新增
- 单击播放画面：暂停 / 继续（双击全屏保持不变，300ms 双击判定消歧）

### 修复
- 点击进度条轨道可直接定位播放进度（此前点击只会小幅跳动，只能拖动）

### 其他
- 功能定位说明更新：目前以视频投屏为主（看番 / 追剧类 App 兼容性较好，如 Lanerc）；音乐、图片与直播投屏正在规划中（关于页 / 主页引导 / README 同步更新）

## [0.1.6] - 2026-08-08

### 改进
- 「检查更新」按钮移到「关于」区（与「项目主页」并排），更符合直觉
- 更新下载升级为 8 连接 + IDM 式动态分块：小任务队列 + 定位写，快的连接自动多干活，慢的连接少干——比固定等分更吃满带宽；小文件（<2MB）自动回退单线程避免开销

## [0.1.5] - 2026-08-08

### 修复
- 系统触发的关闭（Windows 关机/注销、安装器 Restart Manager 关闭请求）不再走「最小化到托盘」逻辑，直接保存配置并退出进程——旧版运行中覆盖安装时应用不会再赖在托盘挡住 exe 替换，关机时也不再残留托盘进程

## [0.1.4] - 2026-08-08

### 修复
- 设置页「自动检查更新」卡片未加入布局导致不显示（0.1.2 / 0.1.3 均受影响）

### 改进
- 更新提示弹窗渲染更新说明（Markdown → 富文本，不再显示原始 md 文本）
- 更新下载改为 4 段 Range 并发下载，速度显著提升；服务器不支持 Range 时自动回退单线程

## [0.1.3] - 2026-08-08

### 修复
- 关于页版本号显示错误：`APP_VERSION` 改为跟随打包版本（此前硬编码 0.1.0，任何版本都显示 0.1.0）
- 安装程序覆盖升级不彻底：安装前自动关闭正在运行的轻投，并清空旧 `_internal` 目录，避免旧文件残留导致新版功能不出现（此前已运行的旧程序会锁住 exe，安装器替换失败）

## [0.1.2] - 2026-08-08

### 新增
- 自动更新：启动时自动检查 GitHub 是否有新版本，发现后一键下载并安装（默认开启，可在设置中关闭）
- 设置页新增「检查更新」按钮，可随时手动检查并更新
- Release 说明：发布时自动带上本次更新内容（即本文件对应小节）

## [0.1.1] - 2026-08-08

### 修复
- 开机自启不再弹命令行窗口（改用 pythonw + `--autostart` 静默启动到系统托盘）

## [0.1.0] - 2026-08-08

### 首个公开版本
- DLNA 投屏接收：手机投视频 / 音乐 / 图片到电脑，libmpv 播放
- 投屏即进播放器页并显示缓冲动画，体感秒开
- 流媒体兼容层：加密 HLS、漫画/图文番、PNG 伪装分片、防盗链、直链代理，换番不报错
- 播放失败友好提示（区分防盗链 403 / 链接失效 / 密钥拦截 / 内容异常 / 网络超时）
- Win11 Fluent 风格界面，中英双语，16:9 默认窗口
- 音频输出设备选择、开机自启（托盘静默）、系统托盘常驻
- 播放进度实时回传给手机端
- 打包：PyInstaller 便携版 + Inno Setup 安装版，GitHub Actions 自动发布
