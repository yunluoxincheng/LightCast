# 更新日志

发布流程（GitHub Actions）会在打 `v*` 标签时自动提取**当前版本对应的小节**作为 Release 说明。新增版本时请按下面的格式在本文件顶部添加小节。

格式：

```markdown
## [版本号] - 日期

### 新增 / 修复 / 其他
- 条目
```

## [0.1.7] - 2026-08-08

### 新增
- 单击播放画面：暂停 / 继续（双击全屏保持不变，300ms 双击判定消歧）

### 修复
- 点击进度条轨道可直接定位播放进度（此前点击只会小幅跳动，只能拖动）

### 其他
- 功能定位说明更新：目前以视频投屏为主（看番 / 追剧类 App 兼容性较好，如 Lanerc）；音乐、图片与直播投屏正在规划中（关于页 / 主页引导 / README 同步更新）

## [0.1.6] - 2026-08-08

### 改进
- 「检查更新」按钮移到「关于」区（与「项目主页」并排），更符合直觉
- 更新下载升级为 8 连接 + IDM 式动态分块：小任务队列 + 定位写，快的连接自动多干活，慢的连接少干——比固定等分更吃满带宽；小文件（<2MB）自动回退单线程避免开销

## [0.1.5] - 2026-08-08

### 修复
- 系统触发的关闭（Windows 关机/注销、安装器 Restart Manager 关闭请求）不再走「最小化到托盘」逻辑，直接保存配置并退出进程——旧版运行中覆盖安装时应用不会再赖在托盘挡住 exe 替换，关机时也不再残留托盘进程

## [0.1.4] - 2026-08-08

### 修复
- 设置页「自动检查更新」卡片未加入布局导致不显示（0.1.2 / 0.1.3 均受影响）

### 改进
- 更新提示弹窗渲染更新说明（Markdown → 富文本，不再显示原始 md 文本）
- 更新下载改为 4 段 Range 并发下载，速度显著提升；服务器不支持 Range 时自动回退单线程

## [0.1.3] - 2026-08-08

### 修复
- 关于页版本号显示错误：`APP_VERSION` 改为跟随打包版本（此前硬编码 0.1.0，任何版本都显示 0.1.0）
- 安装程序覆盖升级不彻底：安装前自动关闭正在运行的轻投，并清空旧 `_internal` 目录，避免旧文件残留导致新版功能不出现（此前已运行的旧程序会锁住 exe，安装器替换失败）

## [0.1.2] - 2026-08-08

### 新增
- 自动更新：启动时自动检查 GitHub 是否有新版本，发现后一键下载并安装（默认开启，可在设置中关闭）
- 设置页新增「检查更新」按钮，可随时手动检查并更新
- Release 说明：发布时自动带上本次更新内容（即本文件对应小节）

## [0.1.1] - 2026-08-08

### 修复
- 开机自启不再弹命令行窗口（改用 pythonw + `--autostart` 静默启动到系统托盘）

## [0.1.0] - 2026-08-08

### 首个公开版本
- DLNA 投屏接收：手机投视频 / 音乐 / 图片到电脑，libmpv 播放
- 投屏即进播放器页并显示缓冲动画，体感秒开
- 流媒体兼容层：加密 HLS、漫画/图文番、PNG 伪装分片、防盗链、直链代理，换番不报错
- 播放失败友好提示（区分防盗链 403 / 链接失效 / 密钥拦截 / 内容异常 / 网络超时）
- Win11 Fluent 风格界面，中英双语，16:9 默认窗口
- 音频输出设备选择、开机自启（托盘静默）、系统托盘常驻
- 播放进度实时回传给手机端
- 打包：PyInstaller 便携版 + Inno Setup 安装版，GitHub Actions 自动发布
